---
description: 符号转义规则 — 正文中的特殊符号必须转义
globs: docs/**/*.{md,mdx}
alwaysApply: true
---

# 符号转义规则

正文段落、列表、表格单元格中的以下符号必须用反引号包裹；代码块内无需转义。

## 一、比较运算符与赋值符

| `=` | `<=` | `>=` | `==` | `!=` | `->` | `=>` |

## 二、泛型类型（尖括号）

MDX 解析器将尖括号视为 JSX 标签。`<` 后紧跟大写字母会被解析为标签，需拆分每个泛型类型单独反引号。

| 场景 | 正确 | 错误 |
|---|---|---|
| 单层泛型 | `HashSet<String>` | `HashSet<String>` |
| 嵌套泛型 | `WeakReference` `ThreadLocal<?>` | `WeakReference<ThreadLocal<?>>` |

## 三、JSX 模板字面量（`${...}`）

`${` 会被解析为 JSX 表达式，需转义为 `\${`。如：变量插值 `\`\${business}-pool\``。

## 四、粗体内运算符

`**粗体**` 包裹的内容同样会被 MDX 解析，运算符必须转义。如 `**消费者数量 <= 分区数量**` 应写为 `**消费者数量 \`<=\` 分区数量**`。

## 五、表格单元格

表格单元格属于正文范畴，运算符和泛型必须转义。如 `n \`<=\` 100`、`List\`<String\>`。

## 六、数组与对象字面量

正文或引用块中出现数组、对象字面量时，用反引号包裹整个表达式。如 `sort: [{ sales: desc }, { _id: desc }]`、`search_after: [999, "abc"]`。
