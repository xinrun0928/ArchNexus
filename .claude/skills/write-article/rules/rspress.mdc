---
description: Rspress 基础语法规则
globs: docs/**/*.{md,mdx}
alwaysApply: true
---

# Rspress 基础语法规则

Rspress 使用 [Shiki](https://shiki.style) 在编译期完成语法高亮。

## 正文格式

### 突出内容

| 格式 | 用途 | 示例 |
|---|---|---|
| 行内代码 `` `...` `` | 技术术语、专有名词、命令、配置项 | `CAP`、`Raft`、`threadpool.size` |
| 加粗 `**...**` | 核心原则、重要结论、关键判断 | `一致性是一个从强到弱的光谱` |
| 引用 `>` | 名言、案例、章节引导语 | `> 核心原则：在分区发生时必须在 C 和 A 间二选一` |
| 容器 `:::type` | 提示、警告、补充 | `:::tip` `:::warning` `:::details` |

六种容器类型（必须小写）：`note`、`tip`、`info`、`warning`、`danger`、`details`。使用原则：宁少不多，满屏重点等于没有重点。

### 标题层级

`#` 文档主题 → `##` 子章节 → `###` 细分子题。章节引导语写在 `##` 下方：

```md
## 一致性模型

> 深入：一致不是非黑即白，而是一个光谱
```

### 列表与段落

有序 `1.` 用于步骤，无序 `-` 用于并列项。句间不空行，保持节奏流畅；空行仅用于分隔段落、标题前后、代码块前后。一段 3~5 句为宜。

## 代码块

必须指定语言标识符。

````md
```java
// 错误：无语言标识符
```

```java
// 正确
public class Example {
    public static void main(String[] args) {
        System.out.println("Hello");
    }
}
```
````

meta 属性：`title`（标题）、`lineNumbers=false`（禁用行号）、`wrapCode`（长行换行）、`fold height="200"`（折叠）。

```java title="Example.java" wrapCode
ThreadPoolExecutor executor = new ThreadPoolExecutor(
    corePoolSize, maxPoolSize, keepAliveTime,
    TimeUnit.SECONDS, new LinkedBlockingQueue<>(capacity)
);
```

行注释高亮：`// [!code highlight]`、`// [!code error]`、`// [!code warning]`、`// [!code focus]`。diff 块用 `diff` 语言标识符，`-` 删除 `+` 新增。

## 容器

类型必须小写，禁止 emoji 代替容器。

```mdx
:::tip 最佳实践

内容

:::
```

## 禁止使用 Badge 组件

一律用纯文本替代。

| 场景 | 写法 |
|---|---|
| 标签 + 说明 | `**标签**：说明` |
| 纯标签 | `\|JDK 17+\|` |

## 目录与导航

`_meta.json` 控制侧边栏，类型：`file`、`dir`、`section-header`、`custom-link`、`divider`。目录下仅有文档且无排序要求时可不配置，Rspress 按字母顺序自动生成侧边栏。数字前缀可控制顺序，如 `1-introduction.mdx`。

`_nav.json` 控制导航栏，仅在 `rspress.config.ts` 中未配置 `nav`/`sidebar` 时自动生效。

## 其他规范

- 待办清单用 GFM 语法 `- [ ]` / `- [x]`，不用 Unicode 符号
- 分隔线 `---` 禁止用作装饰性分隔线，内容应直接过渡到下一节标题。唯一例外：需要明确区分正文与延伸内容
- 站内链接不得包含 `/docs` 前缀，不得出现 `.mdx` 后缀
