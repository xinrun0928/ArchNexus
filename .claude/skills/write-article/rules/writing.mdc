---
description: 写作风格规则
globs: docs/**/*.{md,mdx}
alwaysApply: true
---

# 写作风格规则

> `index.mdx` 文件属于导览文件，不受以下规则限制。

> 好的架构文档，不是 API 参数表，不是概念堆砌。它像一场技术评审：从真实问题出发，逐层拆解设计背景、核心原理、关键权衡与落地方案。读者不只是知道「怎么做」，更知道「为什么这么做、什么时候不该这么做」。

## 一、开场

读者在前 30 秒决定要不要继续读。不要上来就介绍本文内容，先把读者拉进问题里。开场至少做到两件：让读者产生代入感 + 意识到问题的重要性。

开场模板只提供思路，示例用于说明方向，**禁止原封不动套用**。每篇文章必须结合主题自行构思真实、具体、不同的切入角度。

| 模板 | 适用场景 | 构思方向 |
|---|---|---|
| 场景切入 | 故障排查、架构设计 | 构思一个真实感强的现场：时间、地点、异常现象、错误判断。不要每次都是「凌晨 2 点」。 |
| 痛点共鸣 | 常见误区、踩坑 | 从高频错误出发，让读者对号入座。 |
| 核心追问 | 原理类、底层机制 | 问一个读者想过但没深究的问题。 |
| 反差对比 | 打破认知 | 找一个普遍误解，证明它是错的。 |
| 悬念制造 | 新概念、复杂设计 | 展示一个反直觉的结果，引发好奇。 |
| 一行代码开场 | 最佳实践、参数分析 | 选一个看似简单但有深意的配置或代码片段。 |
| 数字开场 | 性能分析 | 找一个真实或有代表性的数据。 |
| 历史演进 | 版本差异、技术演化 | 从起点出发，讲清楚为什么要演进。 |

> 一篇文档只用一个开场，不要叠加。

## 二、文章结构

技术文档最大的失败不是写错，而是写乱。推荐两种结构：

**漏斗式展开**：问题背景 → 现有方案不够 → 核心设计思路 → 关键原理拆解 → 实际使用 → 常见问题 → 适用与不适用场景 → 总结。

**阶梯式推进**：最简版本（先跑通）→ 完整版本（加异常处理）→ 生产版本（加监控、限流、熔断）→ 原理版本（解释为什么）→ 调优版本。

## 三、节奏控制

一篇文章从头到尾一个节奏，读者会疲劳。

慢节奏（多追问、多拆解、多解释为什么）适合：核心原理、复杂机制、设计权衡、故障排查。

快节奏（简洁直接）适合：配置步骤、命令说明、参数列表。

一篇 3000 字左右的文章，至少 2~3 次节奏切换：开场快 → 原理慢 → 配置快 → 故障案例慢 → 总结快。

## 四、表达技巧

**多用对比**：回答它比什么好、好在哪、代价是什么、什么场景下更差。

**多用数字**：「当单表超过 5000 万行后，索引维护成本明显上升」比「数据量大时会变慢」更可信。

**多用转折**：「很多人以为……但真正的问题在于……」「表面上是网络问题，实际是线程池阻塞导致的连锁反应」。

**多用类比但不过度**：熔断器像保险丝、限流像高速收费站、消息队列像快递柜。类比只能帮助入门，后面必须回到真实机制。

**善用架构图**：涉及系统设计时，先给整体架构图，再逐步展开。

## 五、代码示例

代码不是越多越好，越「能说明问题」越好。好代码示例：可直接运行、尽量完整、变量名有意义、代码后有解释、能体现真实业务场景。

推荐组织方式：最简示例（先跑通）→ 生产版本（加日志、异常、超时、限流）→ 错误示例（告诉读者为什么危险）。

## 六、架构类文章核心问题

必须回答：它解决了什么问题？为什么不用更简单的方案？它的代价是什么？什么场景下不应该用？

## 七、真实项目视角

推荐加入：线上踩坑案例、压测数据、故障复盘、架构演进过程。

推荐句式：「如果系统 QPS 不高，其实没必要……」「线上已经出现……问题，优先排查……」「这个方案前期很舒服，但业务复杂后会暴露……问题」。

## 八、字数与深度

| 类型 | 字数 | 目标 |
|---|---|---|
| 工具型 | 不限制 | 快速解决问题 |
| 普通文章 | `>= 1500` | 建立完整知识体系 |
| 原理类 | `>= 3000` | 讲透底层机制与设计逻辑 |
| 架构设计类 | `>= 2000` | 讲清 trade-off、适用场景与边界 |

> 不是字数竞赛，是防止文档停在「入门级」却自以为写透了。

普通文章至少回答：技术怎么来的、为什么要这样设计、和同类方案比优劣、实际怎么用。原理类再加：代价是什么、项目怎么落地。架构设计类重点：解决了什么、不用的代价、不适用场景。

## 九、结尾

好的结尾让读者继续思考，不是「本文到这里就结束了」。推荐：抛出新问题、延伸对比、回到开场形成闭环。

## 十、判断标准

写完问自己：读者能不能真正理解为什么这样设计？下次遇到类似问题能不能自己做判断？看完能不能直接落地？是否知道什么时候不该这样做？四个都是「能」，才算写好了。
