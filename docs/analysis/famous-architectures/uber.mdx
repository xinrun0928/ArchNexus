---
title: Uber 架构
description: 实时出行平台的全球分布式架构
---

# Uber 架构

Uber 每天处理数亿次行程匹配请求，支撑着全球 10,000+ 城市 的出行服务。

Uber 是实时调度系统的典型代表。其架构需要同时满足低延迟、强一致性和高可用的要求。

## Uber 的业务特点

```
┌─────────────────────────────────────────────────────────────────┐
│                    Uber 的业务特点                                    │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                                                         │   │
│  │  核心业务场景：                                        │   │
│  │                                                         │   │
│  │  1. 实时匹配：乘客叫车 → 司机接单 → 完成行程        │   │
│  │  2. 动态定价：根据供需关系实时调整价格               │   │
│  │  3. 支付结算：行程结束 → 计算费用 → 完成支付        │   │
│  │  4. 地图导航：最优路径规划 → 实时导航             │   │
│  │                                                         │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                  │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                                                         │   │
│  │  技术挑战：                                            │   │
│  │                                                         │   │
│  │  - 超低延迟：行程匹配需要在数秒内完成               │   │
│  │  - 地理位置：需要实时处理 GPS 位置数据              │   │
│  │  - 高并发：高峰期大量并发请求                       │   │
│  │  - 全球一致性：多城市、多区域的统一调度             │   │
│  │  - 支付准确：金额计算必须精确                      │   │
│  │                                                         │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

## 架构演进

```
┌─────────────────────────────────────────────────────────────────┐
│                    Uber 架构演进                                      │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ 第一代（2010-2013）：单体 + PostgreSQL                  │   │
│  ├─────────────────────────────────────────────────────────┤   │
│  │                                                         │   │
│  │  ┌─────────────────────────────────────────────────┐  │   │
│  │  │  Node.js 后端 + PostgreSQL                       │  │   │
│  │  │                                                 │  │   │
│  │  │  - 一个代码库                                   │  │   │
│  │  │  - 一个数据库                                   │  │   │
│  │  │  - 快速验证商业模式                             │  │   │
│  │  └─────────────────────────────────────────────────┘  │   │
│  │                                                         │   │
│  │  问题：随着业务增长，单体变得臃肿难以维护             │   │
│  │                                                         │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                  │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ 第二代（2013-2016）：微服务 + MySQL                    │   │
│  ├─────────────────────────────────────────────────────────┤   │
│  │                                                         │   │
│  │  ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐         │   │
│  │  │  乘客  │ │  司机  │ │  行程  │ │  支付  │         │   │
│  │  │  服务  │ │  服务  │ │  服务  │ │  服务  │         │   │
│  │  └───┬────┘ └───┬────┘ └───┬────┘ └───┬────┘         │   │
│  │      │         │         │         │                   │   │
│  │      └─────────┼─────────┼─────────┘                   │   │
│  │                │         ▼                             │   │
│  │                │  ┌───────────┐                       │   │
│  │                │  │   MySQL   │                       │   │
│  │                │  │  Schemaless│                       │   │
│  │                │  └───────────┘                       │   │
│  │                │                                       │   │
│  │                                                         │   │
│  │  问题：MySQL 写入成为瓶颈                             │   │
│  │                                                         │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                  │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ 第三代（2016+）：微服务 + 混合存储                     │   │
│  ├─────────────────────────────────────────────────────────┤   │
│  │                                                         │   │
│  │  ┌─────────────────────────────────────────────────┐  │   │
│  │  │                                                 │  │   │
│  │  │  MySQL Cluster │ Cassandra │ Redis │ Postgres  │  │   │
│  │  │                                                 │  │   │
│  │  │  按需选择最适合的存储引擎                        │  │   │
│  │  │                                                 │  │   │
│  │  └─────────────────────────────────────────────────┘  │   │
│  │                                                         │   │
│  │  新增：                                                │   │
│  │  - Kafka：异步消息处理                                 │   │
│  │  - Flink：实时数据流处理                              │   │
│  │  - GraphQL：统一 API 层                              │   │
│  │                                                         │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

## 核心调度系统

```
┌─────────────────────────────────────────────────────────────────┐
│                    Uber 的调度系统架构                               │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                                                         │   │
│  │  DISPATCH（调度服务）：                                │   │
│  │                                                         │   │
│  │  ┌─────────────────────────────────────────────────┐  │   │
│  │  │                                                 │  │   │
│  │  │  1. 接收乘客请求                                  │  │   │
│  │  │  2. 查询附近可用司机（Geofence）                │  │   │
│  │  │  3. 计算最优匹配（距离、评分、动态定价）        │  │   │
│  │  │  4. 发送派单请求给最优司机                      │  │   │
│  │  │  5. 等待司机响应（超时重试）                    │  │   │
│  │  │                                                 │  │   │
│  │  └─────────────────────────────────────────────────┘  │   │
│  │                                                         │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

### 调度服务实现

```java
// 调度服务核心逻辑
@Service
public class DispatchService {
    @Autowired private DriverService driverService;
    @Autowired private PricingService pricingService;
    @Autowired private LocationService locationService;

    // 调度决策：100ms 内必须完成
    public DispatchDecision dispatch(Long passengerId, Location pickup) {
        long startTime = System.currentTimeMillis();

        // 1. 查询附近可用司机（Geofence 查询）
        List<Driver> nearbyDrivers = driverService.findNearby(pickup,
            radiusMeters = 5000);

        if (nearbyDrivers.isEmpty()) {
            return DispatchDecision.noDrivers();
        }

        // 2. 并行计算每个司机的匹配分数
        List<DriverScore> scoredDrivers = nearbyDrivers.parallelStream()
            .map(driver -> calculateScore(driver, pickup))
            .collect(Collectors.toList());

        // 3. 选择最优司机
        DriverScore bestMatch = scoredDrivers.stream()
            .max(Comparator.comparingDouble(DriverScore::getTotalScore))
            .orElseThrow();

        // 4. 尝试派单
        DispatchResult result = attemptDispatch(bestMatch.getDriver(), passengerId);

        long latency = System.currentTimeMillis() - startTime;
        metrics.record("dispatch.latency_ms", latency);

        return result;
    }

    // 计算匹配分数
    private DriverScore calculateScore(Driver driver, Location pickup) {
        // 距离分数：越近越高
        double distanceScore = calculateDistanceScore(
            driver.getCurrentLocation(), pickup);

        // 评分分数：越高越好
        double ratingScore = driver.getRating() / 5.0;

        // 动态定价系数
        double surgeMultiplier = pricingService.getSurgeMultiplier(pickup);

        // 综合分数
        double totalScore =
            distanceScore * 0.6 +
            ratingScore * 0.3 +
            surgeMultiplier * 0.1;

        return new DriverScore(driver, totalScore);
    }
}
```

## 数据存储架构

```
┌─────────────────────────────────────────────────────────────────┐
│                    Uber 的数据存储分层                               │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                                                         │   │
│  │  Schemaless（MySQL）：                                  │   │
│  │  - 行程数据：高频写入，强一致性                       │   │
│  │  - 用户数据：需要事务支持                             │   │
│  │                                                         │   │
│  │  Cassandra：                                            │   │
│  │  - 事件日志：时序数据                                 │   │
│  │  - 分析查询：写多读少                                 │   │
│  │                                                         │   │
│  │  Redis：                                                │   │
│  │  - 司机位置：实时 GPS                                 │   │
│  │  - 会话缓存：低延迟                                  │   │
│  │                                                         │   │
│  │  Druid：                                                │   │
│  │  - 实时分析：监控指标                                 │   │
│  │  - 业务报表                                           │   │
│  │                                                         │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

### 司机位置存储

```java
// Redis GEO 实现司机位置服务
@Service
public class DriverLocationService {
    @Autowired private RedisTemplate<String, String> redisTemplate;

    private static final String GEO_KEY = "driver:locations";

    // 更新司机位置
    public void updateLocation(Long driverId, double longitude, double latitude) {
        redisTemplate.opsForGeo().add(
            GEO_KEY,
            new Point(longitude, latitude),
            driverId.toString()
        );
    }

    // 查询附近司机
    public List<Driver> findNearby(double longitude, double latitude,
                                  double radiusMeters) {
        Circle circle = new Circle(new Point(longitude, latitude),
            new Distance(radiusMeters, MetricsUnit.METERS));

        GeoResults<GeoOperations.GeoLocation<String>> results =
            redisTemplate.opsForGeo().radius(GEO_KEY, circle);

        return results.getContent().stream()
            .map(r -> {
                Long driverId = Long.parseLong(r.getContent().getName());
                return driverService.findById(driverId);
            })
            .collect(Collectors.toList());
    }

    // 司机上线/下线
    public void setAvailable(Long driverId, boolean available) {
        String key = "driver:" + driverId + ":available";
        if (available) {
            redisTemplate.opsForValue().set(key, "1",
                Duration.ofMinutes(5));
        } else {
            redisTemplate.delete(key);
        }
    }
}
```

## 支付系统

```
┌─────────────────────────────────────────────────────────────────┐
│                    Uber 支付系统                                       │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                                                         │   │
│  │  支付系统的核心要求：                                   │   │
│  │                                                         │   │
│  │  1. 金额精确：浮点数计算可能导致金额错误              │   │
│  │  2. 事务一致：支付必须和行程状态同步                 │   │
│  │  3. 容错处理：支付失败不能丢失记录                   │   │
│  │                                                         │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

```java
// 支付服务：使用事务保证一致性
@Service
public class PaymentService {
    @Autowired private PaymentRepository paymentRepository;
    @Autowired private TripRepository tripRepository;
    @Autowired private LedgerService ledgerService;

    @Transactional
    public PaymentResult chargeRide(Trip trip) {
        // 1. 验证行程状态
        if (trip.getStatus() != TripStatus.COMPLETED) {
            throw new IllegalStateException("Trip not completed");
        }

        // 2. 计算费用（使用 BigDecimal 避免精度问题）
        Money fare = pricingService.calculateFare(trip);
        Money platformFee = fare.multiply(PLATFORM_COMMISSION_RATE);
        Money driverPay = fare.subtract(platformFee);

        // 3. 创建支付记录
        Payment payment = new Payment();
        payment.setTripId(trip.getId());
        payment.setAmount(fare.getAmount());
        payment.setCurrency(fare.getCurrency());
        payment.setStatus(PaymentStatus.PENDING);
        paymentRepository.save(payment);

        try {
            // 4. 调用支付网关
            PaymentGatewayResult gatewayResult =
                paymentGateway.charge(trip.getPassenger().getPaymentMethod(),
                                     fare);

            if (gatewayResult.isSuccess()) {
                payment.setStatus(PaymentStatus.COMPLETED);
                payment.setGatewayTransactionId(gatewayResult.getTransactionId());

                // 5. 更新司机账户
                ledgerService.creditDriver(trip.getDriverId(), driverPay);

                return PaymentResult.success(payment);
            } else {
                payment.setStatus(PaymentStatus.FAILED);
                payment.setFailureReason(gatewayResult.getErrorMessage());
                return PaymentResult.failed(gatewayResult.getErrorMessage());
            }
        } catch (PaymentGatewayException e) {
            payment.setStatus(PaymentStatus.RETRY);
            payment.setRetryCount(payment.getRetryCount() + 1);
            return PaymentResult.retry();
        } finally {
            paymentRepository.save(payment);
        }
    }
}
```

## 架构启示

```
┌─────────────────────────────────────────────────────────────────┐
│                    Uber 架构启示                                      │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ 启示一：按需选择存储引擎                                │   │
│  ├─────────────────────────────────────────────────────────┤   │
│  │                                                         │   │
│  │  Uber 从 MySQL 一把梭，到 Schemaless + Cassandra +     │   │
│  │  Redis + Druid，每种数据选择最适合的存储。              │   │
│  │                                                         │   │
│  │  建议：不要迷信 NoSQL，MySQL 依然是大多数场景的         │   │
│  │  最佳选择。                                              │   │
│  │                                                         │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                  │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ 启示二：低延迟系统的设计要点                            │   │
│  ├─────────────────────────────────────────────────────────┤   │
│  │                                                         │   │
│  │  Uber 调度要求 100ms 内完成。关键设计：               │   │
│  │  - 内存优先：Redis 存储热点数据                      │   │
│  │  - 并行计算：充分利用多核                            │   │
│  │  - 简化链路：减少服务调用层级                       │   │
│  │  - 预计算：提前计算可能的路由                       │   │
│  │                                                         │   │
│  │  建议：延迟敏感的链路要专门优化。                      │   │
│  │                                                         │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                  │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ 启示三：金融级系统的金额处理                          │   │
│  ├─────────────────────────────────────────────────────────┤   │
│  │                                                         │   │
│  │  Uber 支付系统展示了一个原则：                        │   │
│  │  金额必须使用 BigDecimal，不能用 double/float。       │   │
│  │  支付必须走事务，不能为了性能牺牲一致性。             │   │
│  │                                                         │   │
│  │  建议：涉及金钱的系统，保守一点总没错。                │   │
│  │                                                         │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

## 总结

Uber 是实时调度系统的典型案例。

**架构演进**：

- 单体验证 → 微服务拆分 → 混合存储

**核心技术**：

- 调度系统：低延迟匹配算法
- 位置服务：Redis GEO 实时查询
- 支付系统：事务保证一致性
- 动态定价：实时供需平衡

**对普通项目的启示**：

- 根据数据特点选择存储引擎
- 低延迟系统要专门优化
- 金融系统金额处理必须精确
